import {
  APP_WHITE,
  APP_BLACK,
  APP_GRAY,
  APP_BLUE,
  APP_ORANGE,
  APP_ACCENT_LIGHT,
  APP_ACCENT_DARK,
} from '@/config'

export enum AppColors {
  WHITE = APP_WHITE,
  BLACK = APP_BLACK,
  GRAY = APP_GRAY,
  BLUE = APP_BLUE,
  ORANGE = APP_ORANGE,
}

export enum AccentColors {
  LIGHT = APP_ACCENT_LIGHT,
  DARK = APP_ACCENT_DARK,
}
